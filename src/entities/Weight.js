class User {
  constructor(params) {
    this.date = params?.date;
    this.id = params?.id;
    this.note = params?.note;
    this.user = params?.user;
    this.weight = params?.weight;
  }
}

export default User;
